import 'package:flutter/material.dart';

import 'layout/body.dart';

class FollowedUserView extends StatelessWidget {
  const FollowedUserView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FollowedUserViewBody();
  }
}
