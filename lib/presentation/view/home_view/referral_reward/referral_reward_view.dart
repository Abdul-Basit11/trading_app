import 'package:flutter/material.dart';

import 'layout/body.dart';

class ReferralRewardView extends StatelessWidget {
  const ReferralRewardView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const ReferralRewardViewBody();
  }
}
