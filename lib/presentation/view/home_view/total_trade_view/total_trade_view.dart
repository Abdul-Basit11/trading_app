import 'package:flutter/material.dart';

import 'layout/body.dart';

class TotalTradeView extends StatelessWidget {
  const TotalTradeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TotalTradeViewBody();
  }
}
