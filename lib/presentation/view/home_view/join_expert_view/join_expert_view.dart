import 'package:flutter/material.dart';

import 'layout/body.dart';

class JoinExpertView extends StatelessWidget {
  const JoinExpertView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return   JoinExpertViewBody();
  }
}
