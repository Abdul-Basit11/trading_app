import 'package:flutter/material.dart';

import 'layout/body.dart';

class WithDrawPinView extends StatelessWidget {
  const WithDrawPinView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const WithDrawViewBody();
  }
}
